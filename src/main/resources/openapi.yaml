openapi: 3.1.0
info:
  title: BlueChat OpenAPI spec
  description: Agent that bridges AI models and BlueChat
  version: 0.0.1
servers:
  - url: http://localhost:8080
tags:
  - name: ping
    description: endpoints related to ping
  - name: bluebubbles
    description: BlueBubbles webhook endpoints
  - name: lxmf
    description: LXMF bridge webhook endpoints
  - name: gcal
    description: Google Calendar OAuth endpoints
  - name: coder
    description: Coder MCP OAuth endpoints
  - name: workflowCallback
    description: Agent workflow callback endpoints
  - name: websiteAccount
    description: Website account management endpoints
  - name: contact
    description: Public help and contact endpoints
  - name: subscription
    description: Payment subscription and billing endpoints
  - name: admin
    description: Admin-only operational endpoints
paths:

  /api/v1/bluebubbles/messageReceived.message:
    post:
      operationId: bluebubblesMessageReceived
      tags:
        - bluebubbles
      summary: BlueBubbles message webhook
      description: Receives BlueBubbles message webhook payloads
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BlueBubblesMessageReceivedRequest'
      responses:
        '200':
          description: Message processed
          content:
            application/json:
              schema:
                type: object
  /api/v1/lxmf/receive.messages:
    post:
      operationId: lxmfReceiveMessages
      tags:
        - lxmf
      summary: LXMF bridge message webhook
      description: Receives normalized inbound LXMF messages from the LXMF bridge.
      parameters:
        - name: X-LXMF-Bridge-Secret
          in: header
          description: Shared secret used by the LXMF bridge to authenticate webhook calls.
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LxmfMessageReceivedRequest'
      responses:
        '200':
          description: Message processed
          content:
            application/json:
              schema:
                type: object
        '401':
          description: Missing or invalid bridge secret
          content:
            application/json:
              schema:
                type: object
  /api/v1/ping/get.ping:
    get:
      operationId: ping
      tags:
        - ping
      summary: Pings the server to see if it is alive
      description: Pings the server and replies with some information about the server if it is alive
      responses:
        '200':    # status code
          description: A JSON object about the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PingResponse"
  /api/v1/gcal/completeOauth.gcal:
    get:
      operationId: gcalCompleteOauth
      tags:
        - gcal
      summary: Completes Google Calendar OAuth flow
      description: Handles the OAuth redirect from Google and exchanges the code for tokens.
      parameters:
        - name: code
          in: query
          description: OAuth authorization code from Google
          required: false
          schema:
            type: string
        - name: state
          in: query
          description: Account key used to associate the OAuth session
          required: false
          schema:
            type: string
        - name: error
          in: query
          description: OAuth error returned by Google when authorization is denied
          required: false
          schema:
            type: string
      responses:
        '303':
          description: Browser redirected to the frontend OAuth result page
          headers:
            Location:
              description: Frontend OAuth result URL
              schema:
                type: string
                format: uri-reference
  /api/v1/coder/completeOauth.coder:
    get:
      operationId: coderCompleteOauth
      tags:
        - coder
      summary: Completes Coder MCP OAuth flow
      description: Handles the OAuth redirect from Coder and exchanges the code for tokens.
      parameters:
        - name: code
          in: query
          description: OAuth authorization code from Coder
          required: false
          schema:
            type: string
        - name: state
          in: query
          description: Signed OAuth state used to associate the Coder login session
          required: false
          schema:
            type: string
        - name: error
          in: query
          description: OAuth error returned by Coder when authorization is denied
          required: false
          schema:
            type: string
      responses:
        '303':
          description: Browser redirected to the frontend OAuth result page
          headers:
            Location:
              description: Frontend OAuth result URL
              schema:
                type: string
                format: uri-reference
  /api/v1/workflowCallback/receive.workflowCallbacks:
    post:
      operationId: workflowCallbackReceive
      tags:
        - workflowCallback
      summary: Receives signed async workflow callbacks
      description: Receives Standard Webhooks-compatible callbacks for agent async workflow completion. The request body is verified as raw bytes before JSON parsing; application/json is recommended, and raw JSON posts without a content type are also accepted.
      parameters:
        - name: callback_id
          in: query
          description: Callback identifier generated by the agent.
          required: true
          schema:
            type: string
        - name: webhook-id
          in: header
          description: Standard Webhooks event identifier.
          required: true
          schema:
            type: string
        - name: webhook-timestamp
          in: header
          description: Standard Webhooks Unix timestamp in seconds.
          required: true
          schema:
            type: string
        - name: webhook-signature
          in: header
          description: Standard Webhooks signature list.
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkflowCallbackPayload'
      responses:
        '200':
          description: Callback accepted or already processed
          content:
            application/json:
              schema:
                type: object
        '400':
          description: Missing or malformed callback request
          content:
            application/json:
              schema:
                type: object
        '401':
          description: Invalid webhook signature
          content:
            application/json:
              schema:
                type: object
        '404':
          description: Unknown callback
          content:
            application/json:
              schema:
                type: object
        '409':
          description: Callback already consumed by another webhook event
          content:
            application/json:
              schema:
                type: object
        '410':
          description: Callback expired
          content:
            application/json:
              schema:
                type: object
        '500':
          description: Callback could not be processed
          content:
            application/json:
              schema:
                type: object
  /api/v1/websiteAccount/get.websiteAccount:
    get:
      operationId: websiteAccountGet
      tags:
        - websiteAccount
      summary: Gets the current website account
      description: Returns the authenticated website account profile and linked BlueChat identities.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current website account
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebsiteAccountResponse'
        '401':
          description: Authentication required
  /api/v1/websiteAccount/listLinkedAccounts.websiteAccountLinks:
    get:
      operationId: websiteAccountListLinkedAccounts
      tags:
        - websiteAccount
      summary: Lists linked website account integrations
      description: Lists linked BlueChat identities and integration status derived from those identities.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Linked account integrations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebsiteLinkedAccountsResponse'
        '401':
          description: Authentication required
  /api/v1/websiteAccount/redeemLink.websiteAccountLinks:
    post:
      operationId: websiteAccountRedeemLink
      tags:
        - websiteAccount
      summary: Redeems a BlueChat sender link token
      description: Links the authenticated website account to a BlueChat identity using a short-lived single-use token.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebsiteAccountRedeemLinkRequest'
      responses:
        '200':
          description: Link redeemed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebsiteAccountRedeemLinkResponse'
        '400':
          description: Missing or invalid token
        '401':
          description: Authentication required
        '404':
          description: Link token not found
        '409':
          description: Link token already redeemed by another account
        '410':
          description: Link token expired
  /api/v1/websiteAccount/deleteLinkedAccount.websiteAccountLinks:
    delete:
      operationId: websiteAccountDeleteLinkedAccount
      tags:
        - websiteAccount
      summary: Deletes a linked integration account
      description: Unlinks an integration account derived from a linked BlueChat identity.
      security:
        - bearerAuth: []
      parameters:
        - name: type
          in: query
          description: Integration account type to unlink.
          required: true
          schema:
            type: string
            enum:
              - gcal
              - coder
        - name: account_key
          in: query
          description: Integration account key to remove.
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Linked integration account deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebsiteAccountDeleteLinkedAccountResponse'
        '401':
          description: Authentication required
        '404':
          description: Linked integration account not found
  /api/v1/contact/get.contactConfig:
    get:
      operationId: contactGetConfig
      tags:
        - contact
      summary: Gets public contact form configuration
      description: Returns the public contact form configuration, including the Cap widget endpoint when contact CAPTCHA is configured.
      responses:
        '200':
          description: Contact form configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactConfigResponse'
  /api/v1/contact/create.contactMessages:
    post:
      operationId: contactCreateMessage
      tags:
        - contact
      summary: Creates a public contact message
      description: Verifies the Cap token and creates a Linear help/contact issue for admin review.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContactMessageRequest'
      responses:
        '200':
          description: Contact message accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactMessageResponse'
        '400':
          description: Missing fields or invalid Cap token
        '503':
          description: Contact form or Cap verification is not configured
  /api/v1/subscription/list.subscriptionPlans:
    get:
      operationId: subscriptionListPlans
      tags:
        - subscription
      summary: Lists available subscription plans
      description: Lists active billing plans and the default subscription provider.
      responses:
        '200':
          description: Available subscription plans
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionPlansResponse'
  /api/v1/subscription/get.subscription:
    get:
      operationId: subscriptionGet
      tags:
        - subscription
      summary: Gets current subscription status
      description: Returns billing and entitlement state for the authenticated website account.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current subscription status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionSummaryResponse'
        '401':
          description: Authentication required
  /api/v1/subscription/createCheckout.subscriptionCheckouts:
    post:
      operationId: subscriptionCreateCheckout
      tags:
        - subscription
      summary: Creates a subscription checkout session
      description: Creates a provider checkout session for the authenticated website account.
      security:
        - bearerAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubscriptionCheckoutRequest'
      responses:
        '200':
          description: Checkout created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionCheckoutResponse'
        '400':
          description: Invalid checkout request
        '401':
          description: Authentication required
  /api/v1/subscription/createPortal.subscriptionPortals:
    post:
      operationId: subscriptionCreatePortal
      tags:
        - subscription
      summary: Creates a subscription management portal session
      description: Creates a provider portal session for the authenticated website account.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Portal session created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionPortalResponse'
        '401':
          description: Authentication required
        '404':
          description: No subscription found
  /api/v1/subscription/receiveWebhook.subscriptionProviderEvents:
    post:
      operationId: subscriptionReceiveWebhook
      tags:
        - subscription
      summary: Receives payment provider webhook events
      description: Receives raw provider webhook payloads. The provider adapter validates signatures before processing.
      parameters:
        - name: provider
          in: query
          description: Subscription provider key, such as btcpay or stripe.
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Webhook processed, ignored, or duplicate
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionProviderWebhookResponse'
        '401':
          description: Invalid provider webhook signature
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionProviderWebhookResponse'
  /api/v1/admin/list.subscriptions:
    get:
      operationId: adminListSubscriptions
      tags:
        - admin
      summary: Lists subscription state for admins
      description: Returns payment subscription stats and recent subscription rows.
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 100
      responses:
        '200':
          description: Subscription admin list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSubscriptionListResponse'
        '401':
          description: Authentication required
        '403':
          description: Admin role required
  /api/v1/admin/sync.subscription:
    post:
      operationId: adminSyncSubscription
      tags:
        - admin
      summary: Syncs one provider subscription
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminSubscriptionActionRequest'
      responses:
        '200':
          description: Subscription synced
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSubscriptionActionResponse'
        '400':
          description: Invalid request
        '401':
          description: Authentication required
        '403':
          description: Admin role required
        '404':
          description: Subscription not found
  /api/v1/admin/suspend.subscription:
    post:
      operationId: adminSuspendSubscription
      tags:
        - admin
      summary: Suspends one provider subscription
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminSubscriptionActionRequest'
      responses:
        '200':
          description: Subscription suspended
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSubscriptionActionResponse'
        '400':
          description: Invalid request
        '401':
          description: Authentication required
        '403':
          description: Admin role required
        '404':
          description: Subscription not found
  /api/v1/admin/unsuspend.subscription:
    post:
      operationId: adminUnsuspendSubscription
      tags:
        - admin
      summary: Unsuspends one provider subscription
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminSubscriptionActionRequest'
      responses:
        '200':
          description: Subscription unsuspended
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSubscriptionActionResponse'
        '400':
          description: Invalid request
        '401':
          description: Authentication required
        '403':
          description: Admin role required
        '404':
          description: Subscription not found
  /api/v1/admin/grantPremium.subscription:
    post:
      operationId: adminGrantPremium
      tags:
        - admin
      summary: Grants manual premium access
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminPremiumGrantRequest'
      responses:
        '200':
          description: Manual premium granted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSubscriptionActionResponse'
        '400':
          description: Invalid request
        '401':
          description: Authentication required
        '403':
          description: Admin role required
        '404':
          description: Account not found
  /api/v1/admin/revokePremium.subscription:
    post:
      operationId: adminRevokePremium
      tags:
        - admin
      summary: Revokes manual premium access
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminPremiumGrantRequest'
      responses:
        '200':
          description: Manual premium revoked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSubscriptionActionResponse'
        '400':
          description: Invalid request
        '401':
          description: Authentication required
        '403':
          description: Admin role required
        '404':
          description: Account not found
  /api/v1/admin/list.accountBlocks:
    get:
      operationId: adminListAccountBlocks
      tags:
        - admin
      summary: Lists blocked agent accounts
      description: Returns recently blocked canonical accounts and their linked identities.
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 100
      responses:
        '200':
          description: Blocked accounts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminAccountBlockListResponse'
        '401':
          description: Authentication required
        '403':
          description: Admin role required
  /api/v1/admin/block.account:
    post:
      operationId: adminBlockAccount
      tags:
        - admin
      summary: Blocks an account from agent processing
      description: Blocks a canonical account resolved by account id, website identity, or transport identity.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminAccountBlockRequest'
      responses:
        '200':
          description: Account blocked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminAccountBlockResponse'
        '400':
          description: Invalid request
        '401':
          description: Authentication required
        '403':
          description: Admin role required
        '404':
          description: Account not found
  /api/v1/admin/unblock.account:
    post:
      operationId: adminUnblockAccount
      tags:
        - admin
      summary: Unblocks an account for agent processing
      description: Removes the processing block from a canonical account resolved by account id, website identity, or transport identity.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminAccountBlockRequest'
      responses:
        '200':
          description: Account unblocked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminAccountBlockResponse'
        '400':
          description: Invalid request
        '401':
          description: Authentication required
        '403':
          description: Admin role required
        '404':
          description: Account not found
  /api/v1/admin/get.statistics:
    get:
      operationId: adminGetStatistics
      tags:
        - admin
      summary: Gets recent agent statistics
      description: Returns message, model, tool, and active-user statistics for an admin-selected time period.
      security:
        - bearerAuth: []
      parameters:
        - name: from
          in: query
          description: Inclusive start of the statistics period. Defaults to 24 hours before now.
          required: false
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: Exclusive end of the statistics period. Defaults to now.
          required: false
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Admin statistics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminStatsResponse'
        '400':
          description: Invalid time period
        '401':
          description: Authentication required
        '403':
          description: Admin role required
  /api/v1/admin/get.rate_limit_usage:
    get:
      operationId: adminGetRateLimitUsage
      tags:
        - admin
      summary: Gets current rate-limit usage
      description: Returns current usage for app rate limits, including daily assistant response usage by account.
      security:
        - bearerAuth: []
      parameters:
        - name: limit_key
          in: query
          description: Rate limit key to inspect. Defaults to daily assistant response usage.
          required: false
          schema:
            type: string
        - name: limit
          in: query
          description: Maximum number of usage rows to return.
          required: false
          schema:
            type: integer
            format: int32
            default: 50
            minimum: 1
            maximum: 500
      responses:
        '200':
          description: Admin rate-limit usage
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminRateLimitUsageResponse'
        '401':
          description: Authentication required
        '403':
          description: Admin role required
  /api/v1/admin/list.feedback:
    get:
      operationId: adminListFeedback
      tags:
        - admin
      summary: Lists agent feedback
      description: Legacy feedback inbox endpoint. New user feedback is created as Linear issues instead of Postgres rows.
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Feedback read state to list. Defaults to unread.
          required: false
          schema:
            type: string
            enum:
              - all
              - unread
              - read
            default: unread
        - name: limit
          in: query
          description: Maximum number of feedback items to return.
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 250
            default: 100
      responses:
        '200':
          description: Feedback inbox items
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminFeedbackListResponse'
        '401':
          description: Authentication required
        '403':
          description: Admin role required
  /api/v1/admin/markRead.feedback:
    post:
      operationId: adminMarkFeedbackRead
      tags:
        - admin
      summary: Marks feedback as read
      description: Marks a feedback inbox item as read.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminFeedbackStatusUpdateRequest'
      responses:
        '200':
          description: Feedback item after the update
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminFeedbackItem'
        '400':
          description: Missing feedback id
        '401':
          description: Authentication required
        '403':
          description: Admin role required
        '404':
          description: Feedback item not found
  /api/v1/admin/markUnread.feedback:
    post:
      operationId: adminMarkFeedbackUnread
      tags:
        - admin
      summary: Marks feedback as unread
      description: Marks a feedback inbox item as unread.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminFeedbackStatusUpdateRequest'
      responses:
        '200':
          description: Feedback item after the update
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminFeedbackItem'
        '400':
          description: Missing feedback id
        '401':
          description: Authentication required
        '403':
          description: Admin role required
        '404':
          description: Feedback item not found


components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    WebsiteAccountProfile:
      type: object
      required:
        - subject
        - account_id
      properties:
        subject:
          type: string
          nullable: true
        account_id:
          type: string
        email:
          type: string
          nullable: true
        preferred_username:
          type: string
          nullable: true
        display_name:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
          nullable: true
        updated_at:
          type: string
          format: date-time
          nullable: true
    WebsiteAccountIdentity:
      type: object
      required:
        - type
        - identifier
        - normalized_identifier
      properties:
        type:
          type: string
          enum:
            - imessage_email
            - imessage_phone
            - lxmf_address
        identifier:
          type: string
        normalized_identifier:
          type: string
    WebsiteAccountLink:
      type: object
      required:
        - link_id
        - account_id
        - identities
      properties:
        link_id:
          type: string
        account_id:
          type: string
          description: Canonical account id shared across chat identities and integrations.
        identities:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteAccountIdentity'
        chat_guid:
          type: string
          nullable: true
        sender:
          type: string
          nullable: true
        service:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
          nullable: true
    WebsiteAccountResponse:
      type: object
      required:
        - account
        - links
      properties:
        account:
          $ref: '#/components/schemas/WebsiteAccountProfile'
        links:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteAccountLink'
    WebsiteCalendarAccountSummary:
      type: object
      required:
        - account_id
        - account_key
      properties:
        account_id:
          type: string
        account_key:
          type: string
        email:
          type: string
          nullable: true
    WebsiteLinkedIntegrationAccount:
      type: object
      required:
        - type
        - account_key
        - label
        - unlinkable
      properties:
        type:
          type: string
          enum:
            - gcal
            - coder
        account_key:
          type: string
        email:
          type: string
          nullable: true
        label:
          type: string
        unlinkable:
          type: boolean
    WebsiteModelOption:
      type: object
      required:
        - model
        - label
        - provider
        - enabled
      properties:
        model:
          type: string
          description: Stable model option key, such as local, chatgpt, claude, or gemini.
        label:
          type: string
        provider:
          type: string
          description: Model provider key, such as local, chatgpt, claude, or gemini.
        enabled:
          type: boolean
          description: Whether this option can currently be used by the model picker.
    WebsiteModelAccessSummary:
      type: object
      required:
        - is_premium
        - current_model
        - current_model_label
        - model_selection_allowed
        - model_selection_configurable
        - available_models
      properties:
        account_id:
          type: string
          nullable: true
          description: Canonical account id used by the model picker.
        is_premium:
          type: boolean
        current_model:
          type: string
          description: Read-only current model key exposed to the website.
        current_model_label:
          type: string
        model_selection_allowed:
          type: boolean
          description: Whether this account is allowed to choose models once model selection is enabled.
        model_selection_configurable:
          type: boolean
          description: Whether model selection can currently be changed from the website.
        read_only_reason:
          type: string
          nullable: true
        available_models:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteModelOption'
    WebsiteIntegrationSummary:
      type: object
      required:
        - link
        - coder_linked
        - gcal_accounts
        - linked_accounts
        - model_access
      properties:
        link:
          $ref: '#/components/schemas/WebsiteAccountLink'
        coder_linked:
          type: boolean
        gcal_accounts:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteCalendarAccountSummary'
        linked_accounts:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteLinkedIntegrationAccount'
        model_access:
          $ref: '#/components/schemas/WebsiteModelAccessSummary'
    WebsiteLinkedAccountsResponse:
      type: object
      required:
        - account
        - integrations
      properties:
        account:
          $ref: '#/components/schemas/WebsiteAccountProfile'
        integrations:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteIntegrationSummary'
    WebsiteAccountRedeemLinkRequest:
      type: object
      required:
        - token
      properties:
        token:
          type: string
    WebsiteAccountRedeemLinkResponse:
      type: object
      required:
        - status
        - link
      properties:
        status:
          type: string
        link:
          $ref: '#/components/schemas/WebsiteAccountLink'
    WebsiteAccountDeleteLinkedAccountResponse:
      type: object
      required:
        - deleted
      properties:
        deleted:
          type: boolean
    ContactConfigResponse:
      type: object
      required:
        - enabled
        - captcha_required
        - captcha_configured
      properties:
        enabled:
          type: boolean
        captcha_required:
          type: boolean
        captcha_configured:
          type: boolean
        cap_api_endpoint:
          type: string
          nullable: true
          description: Public Cap widget endpoint, usually https://cap.bre.land/{site-key}/.
    ContactMessageRequest:
      type: object
      required:
        - name
        - email
        - subject
        - message
        - cap_token
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        email:
          type: string
          minLength: 3
          maxLength: 320
        subject:
          type: string
          minLength: 1
          maxLength: 200
        message:
          type: string
          minLength: 1
          maxLength: 5000
        cap_token:
          type: string
          minLength: 1
          description: Token emitted by the Cap widget solve event.
    ContactMessageResponse:
      type: object
      required:
        - status
        - message_id
      properties:
        status:
          type: string
        message_id:
          type: string
          description: Linear issue identifier or ID for the accepted contact request.
    SubscriptionPlan:
      type: object
      required:
        - key
        - display_name
        - price_amount
        - currency
        - billing_interval
        - provider
        - active
      properties:
        key:
          type: string
        display_name:
          type: string
        description:
          type: string
          nullable: true
        price_amount:
          type: string
          description: Decimal plan price as configured by the app.
        currency:
          type: string
        billing_interval:
          type: string
        provider:
          type: string
          description: Subscription provider key, such as btcpay or stripe.
        active:
          type: boolean
    SubscriptionPlansResponse:
      type: object
      required:
        - default_provider
        - plans
      properties:
        default_provider:
          type: string
        plans:
          type: array
          items:
            $ref: '#/components/schemas/SubscriptionPlan'
    SubscriptionCheckoutRequest:
      type: object
      properties:
        plan_key:
          type: string
          nullable: true
        provider:
          type: string
          description: Subscription provider key, such as btcpay or stripe.
          nullable: true
    SubscriptionCheckoutResponse:
      type: object
      required:
        - checkout_session_id
        - provider
        - plan_key
        - checkout_url
      properties:
        checkout_session_id:
          type: string
        provider:
          type: string
          description: Subscription provider key, such as btcpay or stripe.
        plan_key:
          type: string
        checkout_url:
          type: string
          format: uri-reference
        expires_at:
          type: string
          format: date-time
          nullable: true
    SubscriptionPortalResponse:
      type: object
      required:
        - provider
        - portal_url
      properties:
        provider:
          type: string
          description: Subscription provider key, such as btcpay or stripe.
        portal_url:
          type: string
          format: uri-reference
    SubscriptionSummaryResponse:
      type: object
      required:
        - account_id
        - is_premium
        - entitlement_source
        - plans
        - subscriptions
      properties:
        account_id:
          type: string
        is_premium:
          type: boolean
        entitlement_source:
          type: string
        premium_until:
          type: string
          format: date-time
          nullable: true
        plans:
          type: array
          items:
            $ref: '#/components/schemas/SubscriptionPlan'
        subscriptions:
          type: array
          items:
            $ref: '#/components/schemas/AdminSubscriptionItem'
        latest_checkout:
          type: string
          nullable: true
    SubscriptionProviderWebhookResponse:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: string
        message:
          type: string
          nullable: true
        account_id:
          type: string
          nullable: true
        subscription_id:
          type: string
          nullable: true
    AdminSubscriptionStats:
      type: object
      required:
        - total_subscriptions
        - active_subscriptions
        - past_due_subscriptions
        - inactive_subscriptions
        - monthly_recurring_amount
        - currency
      properties:
        total_subscriptions:
          type: integer
          format: int64
        active_subscriptions:
          type: integer
          format: int64
        past_due_subscriptions:
          type: integer
          format: int64
        inactive_subscriptions:
          type: integer
          format: int64
        monthly_recurring_amount:
          type: string
        currency:
          type: string
    AdminSubscriptionItem:
      type: object
      required:
        - subscription_id
        - account_id
        - provider
        - plan_key
        - status
        - updated_at
      properties:
        subscription_id:
          type: string
        account_id:
          type: string
        account_bucket:
          type: string
          nullable: true
        provider:
          type: string
          description: Subscription provider key, such as btcpay or stripe.
        plan_key:
          type: string
        status:
          type: string
        provider_status:
          type: string
          nullable: true
        provider_subscription_id:
          type: string
          nullable: true
        provider_customer_id:
          type: string
          nullable: true
        current_period_end:
          type: string
          format: date-time
          nullable: true
        grace_period_end:
          type: string
          format: date-time
          nullable: true
        management_url:
          type: string
          nullable: true
        last_synced_at:
          type: string
          format: date-time
          nullable: true
        updated_at:
          type: string
          format: date-time
    AdminSubscriptionListResponse:
      type: object
      required:
        - generated_at
        - stats
        - subscriptions
      properties:
        generated_at:
          type: string
          format: date-time
        stats:
          $ref: '#/components/schemas/AdminSubscriptionStats'
        subscriptions:
          type: array
          items:
            $ref: '#/components/schemas/AdminSubscriptionItem'
    AdminSubscriptionActionRequest:
      type: object
      required:
        - subscription_id
      properties:
        subscription_id:
          type: string
        reason:
          type: string
          nullable: true
    AdminPremiumGrantRequest:
      type: object
      required:
        - account_id
      properties:
        account_id:
          type: string
    AdminAccountBlockTargetType:
      type: string
      enum:
        - account_id
        - imessage_email
        - imessage_phone
        - lxmf_address
        - website_subject
        - website_email
    AdminAccountBlockRequest:
      type: object
      required:
        - target
        - target_type
      properties:
        target:
          type: string
          description: Account id or identity value to block or unblock.
        target_type:
          $ref: '#/components/schemas/AdminAccountBlockTargetType'
        reason:
          type: string
          nullable: true
          maxLength: 1000
    AdminBlockedAccountItem:
      type: object
      required:
        - account_id
        - account_bucket
        - blocked
        - identities
      properties:
        account_id:
          type: string
        account_bucket:
          type: string
        blocked:
          type: boolean
        reason:
          type: string
          nullable: true
        blocked_at:
          type: string
          format: date-time
          nullable: true
        blocked_by:
          type: string
          nullable: true
        website_email:
          type: string
          nullable: true
        website_subject:
          type: string
          nullable: true
        identities:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteAccountIdentity'
    AdminAccountBlockListResponse:
      type: object
      required:
        - accounts
      properties:
        accounts:
          type: array
          items:
            $ref: '#/components/schemas/AdminBlockedAccountItem'
    AdminAccountBlockResponse:
      type: object
      required:
        - status
        - account
      properties:
        status:
          type: string
        account:
          $ref: '#/components/schemas/AdminBlockedAccountItem'
    AdminSubscriptionActionResponse:
      type: object
      required:
        - status
      properties:
        status:
          type: string
        account_id:
          type: string
          nullable: true
        subscription_id:
          type: string
          nullable: true
        subscription:
          $ref: '#/components/schemas/AdminSubscriptionItem'
    AdminStatsPeriod:
      type: object
      required:
        - from
        - to
        - bucket_size
      properties:
        from:
          type: string
          format: date-time
        to:
          type: string
          format: date-time
        bucket_size:
          type: string
          enum:
            - hour
            - day
    AdminModelStats:
      type: object
      required:
        - model_key
        - model_label
        - responses_model
        - is_premium
        - message_count
        - active_users
        - percentage
      properties:
        model_key:
          type: string
        model_label:
          type: string
        responses_model:
          type: string
        is_premium:
          type: boolean
        message_count:
          type: integer
          format: int64
        active_users:
          type: integer
          format: int64
        percentage:
          type: number
          format: double
    AdminBucketModelStats:
      type: object
      required:
        - model_key
        - model_label
        - message_count
      properties:
        model_key:
          type: string
        model_label:
          type: string
        message_count:
          type: integer
          format: int64
    AdminSenderStats:
      type: object
      required:
        - account_key_hash
        - account_bucket
        - message_count
        - percentage
        - last_seen_at
        - models
      properties:
        account_key_hash:
          type: string
          description: SHA-256 hash of the sender/account bucket used for metrics grouping.
        account_bucket:
          type: string
          description: Short stable display bucket derived from the account hash.
        message_count:
          type: integer
          format: int64
        percentage:
          type: number
          format: double
        last_seen_at:
          type: string
          format: date-time
        models:
          type: array
          items:
            $ref: '#/components/schemas/AdminBucketModelStats'
    AdminToolStats:
      type: object
      required:
        - tool_name
        - tool_category
        - call_count
        - successful_calls
        - failed_calls
        - active_users
        - success_rate
        - average_duration_ms
        - last_used_at
        - percentage
      properties:
        tool_name:
          type: string
        tool_category:
          type: string
          description: Coarse tool grouping such as bluebubbles, google_calendar, coder, coder_mcp, memory, website, or other.
        call_count:
          type: integer
          format: int64
        successful_calls:
          type: integer
          format: int64
        failed_calls:
          type: integer
          format: int64
        active_users:
          type: integer
          format: int64
        success_rate:
          type: number
          format: double
        average_duration_ms:
          type: number
          format: double
        last_used_at:
          type: string
          format: date-time
        percentage:
          type: number
          format: double
    AdminToolAccountTypeStats:
      type: object
      required:
        - is_premium
        - call_count
        - successful_calls
        - failed_calls
        - active_users
        - success_rate
        - percentage
      properties:
        is_premium:
          type: boolean
        call_count:
          type: integer
          format: int64
        successful_calls:
          type: integer
          format: int64
        failed_calls:
          type: integer
          format: int64
        active_users:
          type: integer
          format: int64
        success_rate:
          type: number
          format: double
        percentage:
          type: number
          format: double
    AdminStatsBucket:
      type: object
      required:
        - bucket_start
        - bucket_end
        - message_count
        - active_users
        - models
      properties:
        bucket_start:
          type: string
          format: date-time
        bucket_end:
          type: string
          format: date-time
        message_count:
          type: integer
          format: int64
        active_users:
          type: integer
          format: int64
        models:
          type: array
          items:
            $ref: '#/components/schemas/AdminBucketModelStats'
    AdminStatsResponse:
      type: object
      required:
        - period
        - total_messages
        - active_users
        - average_messages_per_user
        - total_tool_calls
        - successful_tool_calls
        - failed_tool_calls
        - tool_success_rate
        - models
        - senders
        - timeline
        - tools
        - tool_account_types
      properties:
        period:
          $ref: '#/components/schemas/AdminStatsPeriod'
        total_messages:
          type: integer
          format: int64
        active_users:
          type: integer
          format: int64
        average_messages_per_user:
          type: number
          format: double
        total_tool_calls:
          type: integer
          format: int64
        successful_tool_calls:
          type: integer
          format: int64
        failed_tool_calls:
          type: integer
          format: int64
        tool_success_rate:
          type: number
          format: double
        models:
          type: array
          items:
            $ref: '#/components/schemas/AdminModelStats'
        senders:
          type: array
          description: Top sender/account buckets by message volume for the selected period.
          items:
            $ref: '#/components/schemas/AdminSenderStats'
        timeline:
          type: array
          items:
            $ref: '#/components/schemas/AdminStatsBucket'
        tools:
          type: array
          description: Tool usage grouped by tool name for the selected period.
          items:
            $ref: '#/components/schemas/AdminToolStats'
        tool_account_types:
          type: array
          description: Tool usage grouped by account type for the selected period.
          items:
            $ref: '#/components/schemas/AdminToolAccountTypeStats'
    AdminRateLimitUsage:
      type: object
      required:
        - limit_key
        - limit_label
        - scope_type
        - scope_key
        - account_id
        - account_bucket
        - is_premium
        - used
        - limit
        - remaining
        - percentage
        - exhausted
        - window_start
        - window_end
        - updated_at
      properties:
        limit_key:
          type: string
          description: Stable app limit key, such as message_responses_per_day.
        limit_label:
          type: string
        scope_type:
          type: string
          description: Generic limit scope type, such as account.
        scope_key:
          type: string
          description: Scope identifier. For account-scoped limits this is the canonical account id.
        account_id:
          type: string
          nullable: true
          description: Canonical agent account id when the scope is an account.
        account_bucket:
          type: string
          nullable: true
          description: Short stable display bucket derived from the account id.
        is_premium:
          type: boolean
        used:
          type: integer
          format: int64
        limit:
          type: integer
          format: int64
        remaining:
          type: integer
          format: int64
        percentage:
          type: number
          format: double
        exhausted:
          type: boolean
        window_start:
          type: string
          format: date-time
        window_end:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    AdminRateLimitUsageResponse:
      type: object
      required:
        - generated_at
        - limit_key
        - usages
      properties:
        generated_at:
          type: string
          format: date-time
        limit_key:
          type: string
        usages:
          type: array
          items:
            $ref: '#/components/schemas/AdminRateLimitUsage'
    AdminFeedbackItem:
      type: object
      required:
        - feedback_id
        - account_id
        - account_bucket
        - submitted_at
        - feedback_text
        - category
        - transport
        - read_status
      properties:
        feedback_id:
          type: string
        account_id:
          type: string
          description: Canonical agent account id that submitted the feedback.
        account_bucket:
          type: string
          description: Short display bucket derived from the canonical account id.
        submitted_at:
          type: string
          format: date-time
        feedback_text:
          type: string
        category:
          type: string
        transport:
          type: string
        sender:
          type: string
          nullable: true
        chat_guid:
          type: string
          nullable: true
        message_guid:
          type: string
          nullable: true
        read_status:
          type: string
          enum:
            - unread
            - read
        read_at:
          type: string
          format: date-time
          nullable: true
    AdminFeedbackListResponse:
      type: object
      required:
        - status
        - items
        - unread_count
        - read_count
        - total_count
      properties:
        status:
          type: string
          enum:
            - all
            - unread
            - read
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminFeedbackItem'
        unread_count:
          type: integer
          format: int64
        read_count:
          type: integer
          format: int64
        total_count:
          type: integer
          format: int64
    AdminFeedbackStatusUpdateRequest:
      type: object
      required:
        - feedback_id
      properties:
        feedback_id:
          type: string
    WorkflowCallbackPayload:
      type: object
      required:
        - type
        - timestamp
        - data
      properties:
        type:
          type: string
          description: Event type, such as agent.async_task.completed or agent.async_task.failed.
        timestamp:
          type: string
          format: date-time
          description: Time the callback event occurred.
        data:
          type: object
          description: Thin callback data with status, summary, IDs, links, artifacts, or errors.
          additionalProperties: true
      additionalProperties: true
    LxmfMessageReceivedRequest:
      type: object
      required:
        - message_id
        - source_hash
        - content
      properties:
        message_id:
          type: string
          description: Stable LXMF message identifier from the bridge.
        source_hash:
          type: string
          description: Hex destination hash of the sender.
        destination_hash:
          type: string
          nullable: true
          description: Hex destination hash that received the message.
        title:
          type: string
          nullable: true
        content:
          type: string
        timestamp:
          type: string
          format: date-time
          nullable: true
        fields:
          type: object
          nullable: true
          additionalProperties: true
        signature_validated:
          type: boolean
          nullable: true
        stamp_valid:
          type: boolean
          nullable: true
      additionalProperties: true
    BlueBubblesMessageReceivedRequest:
      type: object
      required:
        - type
        - data
      properties:
        type:
          type: string
          description: Webhook event type.
          enum:
            - new-message
            - updated-message
            - typing-indicator
        data:
          type: object
          description: Incoming message payload.
          required:
            - originalROWID
            - guid
          properties:
            originalROWID:
              type: integer
            guid:
              type: string
            text:
              type: string
              nullable: true
            attributedBody:
              type: string
              nullable: true
            handle:
              type: object
              required:
                - address
              properties:
                originalROWID:
                  type: integer
                address:
                  type: string
                service:
                  type: string
                uncanonicalizedId:
                  type: string
                  nullable: true
                country:
                  type: string
            handleId:
              type: integer
            otherHandle:
              type: integer
            attachments:
              type: array
              items:
                type: object
                required:
                  - originalROWID
                  - guid
                properties:
                  originalROWID:
                    type: integer
                  guid:
                    type: string
                  uti:
                    type: string
                  mimeType:
                    type: string
                  transferName:
                    type: string
                  totalBytes:
                    type: integer
                  height:
                    type: integer
                  width:
                    type: integer
                  metadata:
                    type: object
                    nullable: true
            subject:
              type: string
              nullable: true
            error:
              type: integer
            dateCreated:
              type: integer
              format: int64
            dateRead:
              type: integer
              nullable: true
              format: int64
            dateDelivered:
              type: integer
              nullable: true
              format: int64
            isDelivered:
              type: boolean
            isFromMe:
              type: boolean
            hasDdResults:
              type: boolean
            isArchived:
              type: boolean
            itemType:
              type: integer
            groupTitle:
              type: string
              nullable: true
            groupActionType:
              type: integer
            balloonBundleId:
              type: string
              nullable: true
            associatedMessageGuid:
              type: string
              nullable: true
            associatedMessageType:
              type: string
              nullable: true
            expressiveSendStyleId:
              type: string
              nullable: true
            threadOriginatorGuid:
              type: string
              nullable: true
            hasPayloadData:
              type: boolean
            chats:
              type: array
              items:
                type: object
                required:
                  - originalROWID
                  - guid
                  - style
                  - chatIdentifier
                  - isArchived
                  - displayName
                properties:
                  originalROWID:
                    type: integer
                  guid:
                    type: string
                  style:
                    type: integer
                  chatIdentifier:
                    type: string
                  isArchived:
                    type: boolean
                  displayName:
                    type: string
            messageSummaryInfo:
              type: object
              nullable: true
            payloadData:
              type: object
              nullable: true
            dateEdited:
              type: integer
              nullable: true
            dateRetracted:
              type: integer
              nullable: true
            partCount:
              type: integer
            tempGuid:
              type: string
              nullable: true
    ErrorModel:
      type: object
      required:
        - message
        - code
      properties:
        message:
          type: string
        code:
          type: integer
          minimum: 100
          maximum: 600
    PingResponse:
      type: object
      properties:
        reply:
          type: string
        timestamp:
          type: string
          format: date-time
