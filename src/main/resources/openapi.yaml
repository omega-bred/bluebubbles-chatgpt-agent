openapi: 3.1.0
info:
  title: BlueBubbles ChatGPT Agent OpenAPI spec
  description: Agent that bridges ChatGPT and iMessage
  version: 0.0.1
servers:
  - url: http://localhost:8080
tags:
  - name: ping
    description: endpoints related to ping
  - name: bluebubbles
    description: BlueBubbles webhook endpoints
  - name: lxmf
    description: LXMF bridge webhook endpoints
  - name: gcal
    description: Google Calendar OAuth endpoints
  - name: coder
    description: Coder MCP OAuth endpoints
  - name: workflowCallback
    description: Agent workflow callback endpoints
  - name: websiteAccount
    description: Website account management endpoints
  - name: admin
    description: Admin-only operational endpoints
paths:

  /api/v1/bluebubbles/messageReceived.message:
    post:
      operationId: bluebubblesMessageReceived
      tags:
        - bluebubbles
      summary: BlueBubbles message webhook
      description: Receives BlueBubbles message webhook payloads
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BlueBubblesMessageReceivedRequest'
      responses:
        '200':
          description: Message processed
          content:
            application/json:
              schema:
                type: object
  /api/v1/lxmf/receive.messages:
    post:
      operationId: lxmfReceiveMessages
      tags:
        - lxmf
      summary: LXMF bridge message webhook
      description: Receives normalized inbound LXMF messages from the LXMF bridge.
      parameters:
        - name: X-LXMF-Bridge-Secret
          in: header
          description: Shared secret used by the LXMF bridge to authenticate webhook calls.
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LxmfMessageReceivedRequest'
      responses:
        '200':
          description: Message processed
          content:
            application/json:
              schema:
                type: object
        '401':
          description: Missing or invalid bridge secret
          content:
            application/json:
              schema:
                type: object
  /api/v1/ping/get.ping:
    get:
      operationId: ping
      tags:
        - ping
      summary: Pings the server to see if it is alive
      description: Pings the server and replies with some information about the server if it is alive
      responses:
        '200':    # status code
          description: A JSON object about the server
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PingResponse"
  /api/v1/gcal/completeOauth.gcal:
    get:
      operationId: gcalCompleteOauth
      tags:
        - gcal
      summary: Completes Google Calendar OAuth flow
      description: Handles the OAuth redirect from Google and exchanges the code for tokens.
      parameters:
        - name: code
          in: query
          description: OAuth authorization code from Google
          required: false
          schema:
            type: string
        - name: state
          in: query
          description: Account key used to associate the OAuth session
          required: false
          schema:
            type: string
        - name: error
          in: query
          description: OAuth error returned by Google when authorization is denied
          required: false
          schema:
            type: string
      responses:
        '303':
          description: Browser redirected to the frontend OAuth result page
          headers:
            Location:
              description: Frontend OAuth result URL
              schema:
                type: string
                format: uri-reference
  /api/v1/coder/completeOauth.coder:
    get:
      operationId: coderCompleteOauth
      tags:
        - coder
      summary: Completes Coder MCP OAuth flow
      description: Handles the OAuth redirect from Coder and exchanges the code for tokens.
      parameters:
        - name: code
          in: query
          description: OAuth authorization code from Coder
          required: false
          schema:
            type: string
        - name: state
          in: query
          description: Signed OAuth state used to associate the Coder login session
          required: false
          schema:
            type: string
        - name: error
          in: query
          description: OAuth error returned by Coder when authorization is denied
          required: false
          schema:
            type: string
      responses:
        '303':
          description: Browser redirected to the frontend OAuth result page
          headers:
            Location:
              description: Frontend OAuth result URL
              schema:
                type: string
                format: uri-reference
  /api/v1/workflowCallback/receive.workflowCallbacks:
    post:
      operationId: workflowCallbackReceive
      tags:
        - workflowCallback
      summary: Receives signed async workflow callbacks
      description: Receives Standard Webhooks-compatible callbacks for agent async workflow completion. The request body is verified as raw bytes before JSON parsing; application/json is recommended, and raw JSON posts without a content type are also accepted.
      parameters:
        - name: callback_id
          in: query
          description: Callback identifier generated by the agent.
          required: true
          schema:
            type: string
        - name: webhook-id
          in: header
          description: Standard Webhooks event identifier.
          required: true
          schema:
            type: string
        - name: webhook-timestamp
          in: header
          description: Standard Webhooks Unix timestamp in seconds.
          required: true
          schema:
            type: string
        - name: webhook-signature
          in: header
          description: Standard Webhooks signature list.
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkflowCallbackPayload'
      responses:
        '200':
          description: Callback accepted or already processed
          content:
            application/json:
              schema:
                type: object
        '400':
          description: Missing or malformed callback request
          content:
            application/json:
              schema:
                type: object
        '401':
          description: Invalid webhook signature
          content:
            application/json:
              schema:
                type: object
        '404':
          description: Unknown callback
          content:
            application/json:
              schema:
                type: object
        '409':
          description: Callback already consumed by another webhook event
          content:
            application/json:
              schema:
                type: object
        '410':
          description: Callback expired
          content:
            application/json:
              schema:
                type: object
        '500':
          description: Callback could not be processed
          content:
            application/json:
              schema:
                type: object
  /api/v1/websiteAccount/get.websiteAccount:
    get:
      operationId: websiteAccountGet
      tags:
        - websiteAccount
      summary: Gets the current website account
      description: Returns the authenticated website account profile and linked iMessage identities.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current website account
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebsiteAccountResponse'
        '401':
          description: Authentication required
  /api/v1/websiteAccount/listLinkedAccounts.websiteAccountLinks:
    get:
      operationId: websiteAccountListLinkedAccounts
      tags:
        - websiteAccount
      summary: Lists linked website account integrations
      description: Lists linked iMessage identities and integration status derived from those identities.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Linked account integrations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebsiteLinkedAccountsResponse'
        '401':
          description: Authentication required
  /api/v1/websiteAccount/redeemLink.websiteAccountLinks:
    post:
      operationId: websiteAccountRedeemLink
      tags:
        - websiteAccount
      summary: Redeems an iMessage sender link token
      description: Links the authenticated website account to an iMessage identity using a short-lived single-use token.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebsiteAccountRedeemLinkRequest'
      responses:
        '200':
          description: Link redeemed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebsiteAccountRedeemLinkResponse'
        '400':
          description: Missing or invalid token
        '401':
          description: Authentication required
        '404':
          description: Link token not found
        '409':
          description: Link token already redeemed by another account
        '410':
          description: Link token expired
  /api/v1/websiteAccount/deleteLinkedAccount.websiteAccountLinks:
    delete:
      operationId: websiteAccountDeleteLinkedAccount
      tags:
        - websiteAccount
      summary: Deletes a linked integration account
      description: Unlinks an integration account derived from a linked iMessage identity.
      security:
        - bearerAuth: []
      parameters:
        - name: type
          in: query
          description: Integration account type to unlink.
          required: true
          schema:
            type: string
            enum:
              - gcal
              - coder
        - name: account_key
          in: query
          description: Integration account key to remove.
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Linked integration account deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebsiteAccountDeleteLinkedAccountResponse'
        '401':
          description: Authentication required
        '404':
          description: Linked integration account not found
  /api/v1/admin/get.statistics:
    get:
      operationId: adminGetStatistics
      tags:
        - admin
      summary: Gets recent agent statistics
      description: Returns message, model, and active-user statistics for an admin-selected time period.
      security:
        - bearerAuth: []
      parameters:
        - name: from
          in: query
          description: Inclusive start of the statistics period. Defaults to 24 hours before now.
          required: false
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: Exclusive end of the statistics period. Defaults to now.
          required: false
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Admin statistics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminStatsResponse'
        '400':
          description: Invalid time period
        '401':
          description: Authentication required
        '403':
          description: Admin role required


components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    WebsiteAccountProfile:
      type: object
      required:
        - subject
        - account_id
      properties:
        subject:
          type: string
          nullable: true
        account_id:
          type: string
        email:
          type: string
          nullable: true
        preferred_username:
          type: string
          nullable: true
        display_name:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
          nullable: true
        updated_at:
          type: string
          format: date-time
          nullable: true
    WebsiteAccountIdentity:
      type: object
      required:
        - type
        - identifier
        - normalized_identifier
      properties:
        type:
          type: string
          enum:
            - imessage_email
            - imessage_phone
            - lxmf_address
        identifier:
          type: string
        normalized_identifier:
          type: string
    WebsiteAccountLink:
      type: object
      required:
        - link_id
        - account_id
        - identities
      properties:
        link_id:
          type: string
        account_id:
          type: string
          description: Canonical account id shared across chat identities and integrations.
        identities:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteAccountIdentity'
        chat_guid:
          type: string
          nullable: true
        sender:
          type: string
          nullable: true
        service:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
          nullable: true
    WebsiteAccountResponse:
      type: object
      required:
        - account
        - links
      properties:
        account:
          $ref: '#/components/schemas/WebsiteAccountProfile'
        links:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteAccountLink'
    WebsiteCalendarAccountSummary:
      type: object
      required:
        - account_id
        - account_key
      properties:
        account_id:
          type: string
        account_key:
          type: string
        email:
          type: string
          nullable: true
    WebsiteLinkedIntegrationAccount:
      type: object
      required:
        - type
        - account_key
        - label
        - unlinkable
      properties:
        type:
          type: string
          enum:
            - gcal
            - coder
        account_key:
          type: string
        email:
          type: string
          nullable: true
        label:
          type: string
        unlinkable:
          type: boolean
    WebsiteModelOption:
      type: object
      required:
        - model
        - label
        - provider
        - enabled
      properties:
        model:
          type: string
          description: Stable model option key, such as local, chatgpt, claude, or gemini.
        label:
          type: string
        provider:
          type: string
        enabled:
          type: boolean
          description: Whether this option can currently be used by the model picker.
    WebsiteModelAccessSummary:
      type: object
      required:
        - plan
        - is_premium
        - current_model
        - current_model_label
        - model_selection_allowed
        - model_selection_configurable
        - available_models
      properties:
        account_id:
          type: string
          nullable: true
          description: Canonical account id used by the model picker.
        plan:
          type: string
          enum:
            - standard
            - premium
        is_premium:
          type: boolean
        current_model:
          type: string
          description: Read-only current model key exposed to the website.
        current_model_label:
          type: string
        model_selection_allowed:
          type: boolean
          description: Whether this plan is allowed to choose models once model selection is enabled.
        model_selection_configurable:
          type: boolean
          description: Whether model selection can currently be changed from the website.
        read_only_reason:
          type: string
          nullable: true
        available_models:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteModelOption'
    WebsiteIntegrationSummary:
      type: object
      required:
        - link
        - coder_linked
        - gcal_accounts
        - linked_accounts
        - model_access
      properties:
        link:
          $ref: '#/components/schemas/WebsiteAccountLink'
        coder_linked:
          type: boolean
        gcal_accounts:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteCalendarAccountSummary'
        linked_accounts:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteLinkedIntegrationAccount'
        model_access:
          $ref: '#/components/schemas/WebsiteModelAccessSummary'
    WebsiteLinkedAccountsResponse:
      type: object
      required:
        - account
        - integrations
      properties:
        account:
          $ref: '#/components/schemas/WebsiteAccountProfile'
        integrations:
          type: array
          items:
            $ref: '#/components/schemas/WebsiteIntegrationSummary'
    WebsiteAccountRedeemLinkRequest:
      type: object
      required:
        - token
      properties:
        token:
          type: string
    WebsiteAccountRedeemLinkResponse:
      type: object
      required:
        - status
        - link
      properties:
        status:
          type: string
        link:
          $ref: '#/components/schemas/WebsiteAccountLink'
    WebsiteAccountDeleteLinkedAccountResponse:
      type: object
      required:
        - deleted
      properties:
        deleted:
          type: boolean
    AdminStatsPeriod:
      type: object
      required:
        - from
        - to
        - bucket_size
      properties:
        from:
          type: string
          format: date-time
        to:
          type: string
          format: date-time
        bucket_size:
          type: string
          enum:
            - hour
            - day
    AdminModelStats:
      type: object
      required:
        - model_key
        - model_label
        - responses_model
        - plan
        - is_premium
        - message_count
        - active_users
        - percentage
      properties:
        model_key:
          type: string
        model_label:
          type: string
        responses_model:
          type: string
        plan:
          type: string
        is_premium:
          type: boolean
        message_count:
          type: integer
          format: int64
        active_users:
          type: integer
          format: int64
        percentage:
          type: number
          format: double
    AdminBucketModelStats:
      type: object
      required:
        - model_key
        - model_label
        - message_count
      properties:
        model_key:
          type: string
        model_label:
          type: string
        message_count:
          type: integer
          format: int64
    AdminSenderStats:
      type: object
      required:
        - account_key_hash
        - account_bucket
        - message_count
        - percentage
        - last_seen_at
        - models
      properties:
        account_key_hash:
          type: string
          description: SHA-256 hash of the sender/account bucket used for metrics grouping.
        account_bucket:
          type: string
          description: Short stable display bucket derived from the account hash.
        message_count:
          type: integer
          format: int64
        percentage:
          type: number
          format: double
        last_seen_at:
          type: string
          format: date-time
        models:
          type: array
          items:
            $ref: '#/components/schemas/AdminBucketModelStats'
    AdminStatsBucket:
      type: object
      required:
        - bucket_start
        - bucket_end
        - message_count
        - active_users
        - models
      properties:
        bucket_start:
          type: string
          format: date-time
        bucket_end:
          type: string
          format: date-time
        message_count:
          type: integer
          format: int64
        active_users:
          type: integer
          format: int64
        models:
          type: array
          items:
            $ref: '#/components/schemas/AdminBucketModelStats'
    AdminStatsResponse:
      type: object
      required:
        - period
        - total_messages
        - active_users
        - average_messages_per_user
        - models
        - senders
        - timeline
      properties:
        period:
          $ref: '#/components/schemas/AdminStatsPeriod'
        total_messages:
          type: integer
          format: int64
        active_users:
          type: integer
          format: int64
        average_messages_per_user:
          type: number
          format: double
        models:
          type: array
          items:
            $ref: '#/components/schemas/AdminModelStats'
        senders:
          type: array
          description: Top sender/account buckets by message volume for the selected period.
          items:
            $ref: '#/components/schemas/AdminSenderStats'
        timeline:
          type: array
          items:
            $ref: '#/components/schemas/AdminStatsBucket'
    WorkflowCallbackPayload:
      type: object
      required:
        - type
        - timestamp
        - data
      properties:
        type:
          type: string
          description: Event type, such as agent.async_task.completed or agent.async_task.failed.
        timestamp:
          type: string
          format: date-time
          description: Time the callback event occurred.
        data:
          type: object
          description: Thin callback data with status, summary, IDs, links, artifacts, or errors.
          additionalProperties: true
      additionalProperties: true
    LxmfMessageReceivedRequest:
      type: object
      required:
        - message_id
        - source_hash
        - content
      properties:
        message_id:
          type: string
          description: Stable LXMF message identifier from the bridge.
        source_hash:
          type: string
          description: Hex destination hash of the sender.
        destination_hash:
          type: string
          nullable: true
          description: Hex destination hash that received the message.
        title:
          type: string
          nullable: true
        content:
          type: string
        timestamp:
          type: string
          format: date-time
          nullable: true
        fields:
          type: object
          nullable: true
          additionalProperties: true
        signature_validated:
          type: boolean
          nullable: true
        stamp_valid:
          type: boolean
          nullable: true
      additionalProperties: true
    BlueBubblesMessageReceivedRequest:
      type: object
      required:
        - type
        - data
      properties:
        type:
          type: string
          description: Webhook event type.
          enum:
            - new-message
            - updated-message
            - typing-indicator
        data:
          type: object
          description: Incoming message payload.
          required:
            - originalROWID
            - guid
          properties:
            originalROWID:
              type: integer
            guid:
              type: string
            text:
              type: string
              nullable: true
            attributedBody:
              type: string
              nullable: true
            handle:
              type: object
              required:
                - address
              properties:
                originalROWID:
                  type: integer
                address:
                  type: string
                service:
                  type: string
                uncanonicalizedId:
                  type: string
                  nullable: true
                country:
                  type: string
            handleId:
              type: integer
            otherHandle:
              type: integer
            attachments:
              type: array
              items:
                type: object
                required:
                  - originalROWID
                  - guid
                properties:
                  originalROWID:
                    type: integer
                  guid:
                    type: string
                  uti:
                    type: string
                  mimeType:
                    type: string
                  transferName:
                    type: string
                  totalBytes:
                    type: integer
                  height:
                    type: integer
                  width:
                    type: integer
                  metadata:
                    type: object
                    nullable: true
            subject:
              type: string
              nullable: true
            error:
              type: integer
            dateCreated:
              type: integer
              format: int64
            dateRead:
              type: integer
              nullable: true
              format: int64
            dateDelivered:
              type: integer
              nullable: true
              format: int64
            isDelivered:
              type: boolean
            isFromMe:
              type: boolean
            hasDdResults:
              type: boolean
            isArchived:
              type: boolean
            itemType:
              type: integer
            groupTitle:
              type: string
              nullable: true
            groupActionType:
              type: integer
            balloonBundleId:
              type: string
              nullable: true
            associatedMessageGuid:
              type: string
              nullable: true
            associatedMessageType:
              type: string
              nullable: true
            expressiveSendStyleId:
              type: string
              nullable: true
            threadOriginatorGuid:
              type: string
              nullable: true
            hasPayloadData:
              type: boolean
            chats:
              type: array
              items:
                type: object
                required:
                  - originalROWID
                  - guid
                  - style
                  - chatIdentifier
                  - isArchived
                  - displayName
                properties:
                  originalROWID:
                    type: integer
                  guid:
                    type: string
                  style:
                    type: integer
                  chatIdentifier:
                    type: string
                  isArchived:
                    type: boolean
                  displayName:
                    type: string
            messageSummaryInfo:
              type: object
              nullable: true
            payloadData:
              type: object
              nullable: true
            dateEdited:
              type: integer
              nullable: true
            dateRetracted:
              type: integer
              nullable: true
            partCount:
              type: integer
            tempGuid:
              type: string
              nullable: true
    ErrorModel:
      type: object
      required:
        - message
        - code
      properties:
        message:
          type: string
        code:
          type: integer
          minimum: 100
          maximum: 600
    PingResponse:
      type: object
      properties:
        reply:
          type: string
        timestamp:
          type: string
          format: date-time
